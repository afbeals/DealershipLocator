import { fetchDealerships } from "./api/dealerships";
import { fetchEmployees } from "./api/employees";
import { fetchLocations } from "./api/locations";

const api = {
  fetchDealerships,
  fetchEmployees,
  fetchLocations
};

export default api;
